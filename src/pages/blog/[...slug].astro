---
import BaseLayout from "../../layouts/BaseLayout.astro";
import { getCollection, render } from "astro:content";

export async function getStaticPaths() {
  const posts = await getCollection("blog");
  return posts.map((post) => ({
    params: { slug: post.id },
    props: { post },
  }));
}

const { post } = Astro.props;
const { Content } = await render(post);
---

<BaseLayout title={post.data.title} description={post.data.description}>
  <article class="section-shell">
    <div class="container-shell">
      <div class="grid gap-10 xl:grid-cols-[1fr_320px]">
        <div class="space-y-10">
          <div class="rounded-[2rem] border border-white/10 bg-white/[0.04] p-8 shadow-[0_24px_80px_rgba(0,0,0,0.22)]">
            <div class="text-sm uppercase tracking-[0.28em] text-amber-300">Blog</div>
            <h1 class="mt-4 text-4xl font-semibold text-white sm:text-5xl">
              {post.data.title}
            </h1>
            <p class="mt-4 max-w-3xl text-lg leading-8 text-stone-300">
              {post.data.description}
            </p>
            <div class="mt-6 flex flex-wrap gap-4 text-sm text-stone-400">
              <time datetime={post.data.pubDate.toISOString()}>
                Published {post.data.pubDate.toLocaleDateString("en-US", {
                  year: "numeric",
                  month: "long",
                  day: "numeric",
                })}
              </time>
              {post.data.updatedDate && (
                <span>
                  Updated {post.data.updatedDate.toLocaleDateString("en-US", {
                    year: "numeric",
                    month: "long",
                    day: "numeric",
                  })}
                </span>
              )}
              <span>Forgefront 3D</span>
            </div>
          </div>

          {post.data.heroImage && (
            <div class="overflow-hidden rounded-[2rem] border border-white/10 bg-white/[0.03]">
              <img
                src={post.data.heroImage}
                alt={post.data.title}
                class="w-full h-72 object-cover"
              />
            </div>
          )}

          <div class="prose prose-invert prose-lg max-w-none">
            <Content />
          </div>

          <footer class="mt-12 border-t border-white/10 pt-8">
            <div class="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between">
              <a href="/blog/" class="btn-secondary w-fit">← Back to Blog</a>
              <a href="/quote/" class="btn-primary w-fit">Get a Quote</a>
            </div>
          </footer>
        </div>

        <aside class="space-y-6">
          <div class="sticky top-24 rounded-[2rem] border border-white/10 bg-white/[0.04] p-8 shadow-[0_18px_40px_rgba(0,0,0,0.20)]">
            <p class="text-xs uppercase tracking-[0.28em] text-amber-300">Need a custom part?</p>
            <h2 class="mt-4 text-2xl font-semibold text-white">
              Get material advice and a quote.
            </h2>
            <p class="mt-4 text-sm leading-6 text-stone-300">
              Upload your file or describe your part, and we’ll recommend the right material, finish, and timeline.
            </p>
            <a href="/quote/" class="btn-primary w-full justify-center mt-6">
              Request a quote
            </a>
          </div>

          <div class="rounded-[2rem] border border-white/10 bg-white/[0.03] p-6">
            <h3 class="text-base font-semibold text-white">What to expect</h3>
            <ul class="mt-4 space-y-3 text-sm leading-6 text-stone-300">
              <li>Fast review by a real person</li>
              <li>Material guidance for strength and finish</li>
              <li>Pickup in Prague or shipping available</li>
            </ul>
          </div>
        </aside>
      </div>
    </div>
  </article>
</BaseLayout>